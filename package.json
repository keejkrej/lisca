{
  "name": "lisca-monorepo",
  "private": true,
  "packageManager": "bun@1.2.22",
  "workspaces": [
    "packages/lisca/typescript",
    "apps/viewer"
  ],
  "scripts": {
    "build": "bun run --cwd packages/lisca/typescript build && bun run --cwd apps/viewer build",
    "dev": "bun run --cwd apps/viewer dev",
    "release": "bun run --cwd apps/viewer release",
    "test": "bun run --cwd packages/lisca/typescript test",
    "typecheck": "bun run --cwd packages/lisca/typescript typecheck && bun run --cwd apps/viewer typecheck && cargo check --workspace"
  }
}

{
  "name": "lisca-monorepo",
  "private": true,
  "packageManager": "bun@1.2.22",
  "workspaces": [
    "packages/lisca/typescript",
    "apps/viewer",
    "apps/annotator",
    "apps/studio"
  ],
  "scripts": {
    "build": "turbo run build",
    "build:viewer": "turbo run build --filter=viewer",
    "build:annotator": "turbo run build --filter=annotator",
    "build:studio": "turbo run build --filter=studio",
    "dev:viewer": "turbo run dev --filter=viewer",
    "dev:annotator": "turbo run dev --filter=annotator",
    "dev:studio": "turbo run dev --filter=studio",
    "release:viewer": "turbo run release --filter=viewer",
    "release:annotator": "turbo run release --filter=annotator",
    "release:studio": "turbo run release --filter=studio",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "check:tauri": "turbo run check:tauri",
    "check:rust": "cargo check --workspace",
    "check": "bun run typecheck && bun run check:rust"
  },
  "devDependencies": {
    "turbo": "^2.9.6"
  }
}

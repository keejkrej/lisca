# Enable all Lisca app profiles by default (aligner, annotator, studio).
# Start one app: COMPOSE_PROFILES=studio docker compose up
COMPOSE_PROFILES=all

# Host bind-mount root (workspace/ and source/ under this path).
LISCA_ROOT=/var/lisca

services:
  - type: web
    name: lisca-landing
    runtime: static
    buildCommand: bun lisca install landing && bun lisca build landing
    envVars:
      - key: TURBO_CONCURRENCY
        value: "1"
    staticPublishPath: apps/landing/web/dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    buildFilter:
      paths:
        - apps/landing/**
        - packages/**
        - assets/**
        - scripts/lisca.mjs
        - package.json
        - bun.lock
        - turbo.json

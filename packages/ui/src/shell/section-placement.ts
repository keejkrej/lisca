/** Sidebar: shrink-to-content, scrollable body. */
export const sidebarSectionClass = "min-h-0 shrink-0";

export const sidebarSectionContentClass =
  "flex min-h-0 flex-col gap-2 overflow-auto";

/** Dock: fill strip cell, centered body. */
export const dockSectionClass = "flex h-full min-h-0 min-w-0 w-full flex-col";

export const dockSectionContentClass =
  "flex min-h-0 flex-1 items-center justify-center space-y-0";

/** Inset padding for sidebar, dock, and viewport region containers. */
export const regionInsetClass = "p-2.5";

/** Spacing between stacked items in region containers. */
export const regionStackGapClass = "gap-2.5";
/**
 * Layout is flat `bg-background` for now. Panels keep a border for structure only.
 */

/** Bordered in-app frame (dock, viewport, nav rail). */
export const surfacePanelClass =
  "rounded-xl border border-border bg-background text-foreground shadow-none";

export type ResultChartColors = {
  text: string;
  mutedText: string;
  grid: string;
  primary: string;
};

export const DEFAULT_RESULT_CHART_COLORS: ResultChartColors = {
  text: "#fafafa",
  mutedText: "#a3a3a3",
  grid: "#525252",
  primary: "#60a5fa",
};

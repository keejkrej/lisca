{
  "name": "@lisca/studio-desktop",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "zig build package -Doptimize=ReleaseSafe",
    "dev": "zig build dev",
    "typecheck": "zig build --global-cache-dir ../../.zig-cache-global test -Dplatform=null && cargo check -p studio-server"
  },
  "devDependencies": {
    "@lisca/studio-web": "workspace:*",
    "@lisca/studio-server": "workspace:*",
    "zero-native": "0.1.9"
  }
}

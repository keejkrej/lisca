{
  "name": "@lisca/annotator-server",
  "private": true,
  "scripts": {
    "build": "cargo build --release -p annotator-server",
    "dev": "PORT=9766 cargo watch -w . -w ../../../crates/lisca -w ../../../Cargo.toml -w ../../../Cargo.lock -x \"run -p annotator-server\"",
    "typecheck": "cargo check -p annotator-server"
  }
}

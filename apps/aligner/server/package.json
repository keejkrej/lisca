{
  "name": "@lisca/aligner-server",
  "private": true,
  "scripts": {
    "build": "cargo build --release -p aligner-server",
    "dev": "PORT=9765 cargo watch -w . -w ../../../crates/lisca -w ../../../Cargo.toml -w ../../../Cargo.lock -x \"run -p aligner-server\"",
    "typecheck": "cargo check -p aligner-server"
  }
}

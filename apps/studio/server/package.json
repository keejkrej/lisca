{
  "name": "@lisca/studio-server",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "cargo build --release -p studio-server",
    "dev": "cargo watch -w . -w ../../../crates/lisca -w ../../../Cargo.toml -w ../../../Cargo.lock -x \"run -p studio-server\"",
    "typecheck": "cargo check -p studio-server"
  }
}

# AGENTS.md

Hand-authored only. Loaded into agent context every session — keep it short and stable.

## Concept

software stack for experimental platform 'live-cell imaging on single cell arrays'

## Documentation

- **`AGENTS.md`** — human-written invariants and pointers (this file).
- **`README.md`** — leave blank (title only). Do not put docs here; humans should not rely on them.
- **`docs/agent/`** — on-demand reference for agents (module conventions, pipelines, patterns). Read when working in that area; do not duplicate long content into `AGENTS.md`.

## Reference for UI and design pattern

- https://github.com/pingdotgg/t3code or ../t3code local clone
